import subprocess


def init_assets(
        assets_container_name,
):
    subprocess.run(
        '{} {}'.format(),
    )


def run_tests(
        assets_container_name,
):
    subprocess.run(
        '{} {}'.format(),
    )


if __name__ == '__main__':
    import sys